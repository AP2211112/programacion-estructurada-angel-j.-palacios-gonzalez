
#include<studio.h>
struct complejo{
    float a;
    float b;
}
// solicita un nuemero complejo y los retorna
struct complejo leercomplejo(){

    scanf(struct complejo);
};

//imprime en la pantalla nemero del formato
void mostrarcomplejo(struct complejo c);
    pritf("el numero complejo es: %f %+f /n", _,_);

//retorna la suma de dos numeros complejos
struct complejo suma(struct c1, struct c2):
    n3=a+a + b+b
    
    

//retorna la multiplicacion de dos numeros complejos 
struct complejo multiplicacion(struct c1, struct c2);

    