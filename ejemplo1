#include <stdio.h>
#include <stdli.h>

int anch;
int main()
{  
		float anc, lar, area, peri;

  printf(¨introdusca ancho¨);
  scanf(¨%f¨, &anc);
**procediminto problema
			lar=anc*3;
			area=anc*lar;
			peri=anc*8;
 **resultados
		printf("El perimetro es% /n",peri);
		printf("El area es %/n",area);

		return 0;
		}
