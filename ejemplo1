//angel j. palacios gonzalez
// matricula
#include <stdio.h>
#include <stdli.h>


int main()
{  
		float anc, lar, area, peri;

  printf(¨introdusca ancho¨);
  scanf(¨%f¨, &anc);
//procediminto problema
			lar=anc*3;
			area=anc*lar;
			peri=anc*8;
//resultados
		printf("El perimetro es%f/n",peri);
		printf("El area es %f/n",area);

		return 0;
		}
